# USB Status

USB Status is a macOS menu bar app that displays current USB and Thunderbolt / USB4 connection information from System Information.

## Build and Run

```bash
./script/build_and_run.sh
```

## Install

```bash
./script/build_and_run.sh --install-verify
```

The app is installed as `/Applications/USB Status.app`.
